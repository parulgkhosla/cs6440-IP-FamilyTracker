import logo from './logo.svg';
import './App.css';

function App() {
  // useEffect(() => {
  //   fetch("/??/home")
  //       .then(res => res.json())
  //       .then(
  //           (result) => {
  //             setIsLoaded(true);
  //             setWords(result.words);
  //           },
  //           // Note: it's important to handle errors here
  //           // instead of a catch() block so that we don't swallow
  //           // exceptions from actual bugs in components.
  //           (error) => {
  //             setIsLoaded(true);
  //             setError(error);
  //           }
  //       )
  // }, [])
  return (
    <div className="App">
      <header className="App-header">
        <p>
          Welcome to Family Health Tracker App.
        </p>
      </header>
        <body>

        </body>
    </div>
  );
}

export default App;
